# 과제 02 - 이진트리 프로그램 구현 보고서

## 1. 제출 파일

| 파일 | 내용 |
|---|---|
| `array_btree.c` | 배열을 이용한 이진트리 구현 |
| `linked_btree.c` | 포인터를 이용한 연결 자료구조 구현 |
| `REPORT.md` | 구현 설명 및 두 구현의 비교 분석 (본 문서) |

컴파일 방법

```
gcc -o array_btree array_btree.c
gcc -o linked_btree linked_btree.c
```

## 2. 입력 형식

이전 과제의 괄호 표기법을 이진트리에 맞게 사용한다. 왼쪽과 오른쪽 자식을 구분한다.

```
NODE                단말 노드 (괄호 없음)
NODE(왼쪽,오른쪽)    왼쪽, 오른쪽 자식이 모두 있는 경우
NODE(왼쪽)           왼쪽 자식만 있는 경우 (NODE(왼쪽,) 도 허용)
NODE(,오른쪽)        왼쪽이 비어 있고 오른쪽 자식만 있는 경우
```

예: `A(B(D,E),C(,F))` 는 A의 왼쪽 자식이 B, 오른쪽 자식이 C이고, B의 자식은 D, E, C는 오른쪽 자식 F만 가지는 트리이다.

이진트리는 자식이 최대 2개이므로 `A(B,C,D)` 처럼 자식을 3개 이상 나열하면 형식 오류로 처리하고 프로그램을 종료한다.

## 3. 구현 방법

### 3.1 배열을 이용한 구현 (`array_btree.c`)

- 루트는 배열 인덱스 1에 저장한다.
- 인덱스 i 노드의 왼쪽 자식은 2i, 오른쪽 자식은 2i+1, 부모는 i/2 이다.
- 빈 칸은 0으로 표시한다.
- 입력 문자열을 재귀적으로 파싱하면서, 저장할 인덱스를 인자로 넘겨 바로 배열에 채운다.
- 출력과 트리 정보 계산은 인덱스 2i, 2i+1을 따라가는 재귀 순회로 처리한다.
- 완전 이진트리 판별: 배열 인덱스는 곧 레벨 순서 번호이므로, 노드가 n개일 때 인덱스 1부터 n까지가 빈틈없이 채워져 있으면 완전 이진트리이다.

### 3.2 연결 자료구조를 이용한 구현 (`linked_btree.c`)

- 노드 구조체는 `data`, `left`, `right` 세 필드만 가진다. (parent 필드 없음)
- 입력 문자열을 재귀적으로 파싱하면서 노드를 `malloc`으로 생성하고 포인터로 연결한다.
- 출력과 트리 정보 계산은 `left`, `right` 포인터를 따라가는 재귀 순회로 처리한다.
- 완전 이진트리 판별: 큐를 이용해 레벨 순서로 순회한다. 빈 자리(NULL)를 한 번 만난 뒤에 실제 노드가 다시 나오면 완전 이진트리가 아니다.

### 3.3 트리 정보 및 형태 판별 기준 (두 구현 공통)

- 높이: 루트의 레벨을 1로 계산한다.
- 차수: 모든 노드의 자식 수 중 최댓값이다. 이진트리이므로 0, 1, 2 중 하나이다.
- 완전 이진트리: 마지막 레벨을 제외한 모든 레벨이 꽉 차 있고, 마지막 레벨은 왼쪽부터 채워진 트리이다.
- 포화 이진트리: 완전 이진트리이면서 노드 수가 2^높이 - 1 인 트리이다.
- 편향 이진트리: 자식이 둘인 노드가 하나도 없는 트리이다. 노드가 1개뿐인 트리는 편향 여부를 따질 수 없으므로 제외했다.

## 4. 실행 결과

두 구현 모두 아래 입력에 대해 같은 결과를 출력했다.

                    | 입력 | 노드 수 | 단말 | 비단말 | 높이 | 차수 | 완전 | 포화 | 편향 |
                    |---|---|---|---|---|---|---|---|---|
| `A(B(D,E),C(,F))` | 6 | 3 | 3 | 3 | 2 | X | X | X |
| `A(B(D,E),C(F,))` | 6 | 3 | 3 | 3 | 2 | O | X | X |
| `A(B(D,E),C(F,G))` | 7 | 4 | 3 | 3 | 2 | O | O | X |
| `A(B(C(D)))` | 4 | 1 | 3 | 4 | 1 | X | X | O |
| `A(B(,C(D)))` | 4 | 1 | 3 | 4 | 1 | X | X | O |
| `A` | 1 | 1 | 0 | 1 | 0 | O | O | X |

배열 구현은 인덱스 계산으로, 연결 구현은 포인터와 큐로 서로 다른 방법을 사용했는데 결과가 모두 일치하므로 두 구현의 판별 로직이 올바르다고 볼 수 있다.

`A(B(D,E),C(,F))` 의 트리 출력 결과는 다음과 같다. (위쪽이 오른쪽 서브트리, 아래쪽이 왼쪽 서브트리)

```
    +---F
+---C
A
    +---E
+---B
    +---D
```

## 5. 메모리 사용량 비교

### 5.1 측정 방법

- 측정 환경: x86-64 Linux, `sizeof(char)` = 1바이트, 포인터 = 8바이트
- 배열: 트리를 담는 데 필요한 최소 배열 크기, 즉 사용된 가장 큰 인덱스까지의 칸 수를 측정했다. 한 칸은 `char` 1바이트이다.
- 연결 자료구조: `sizeof(BNode)` × 노드 수로 계산했다. `sizeof(BNode)` 는 24바이트이다. (`data` 1바이트 + 정렬을 위한 패딩 7바이트 + `left` 8바이트 + `right` 8바이트)
- `malloc` 이 블록마다 추가로 사용하는 관리 공간은 플랫폼마다 달라서 포함하지 않았다. 이를 포함하면 연결 구조의 실제 사용량은 표보다 더 크다.

### 5.2 측정 결과

| 트리 종류 | 입력 | 노드 수 | 배열 사용 칸 | 배열 낭비 칸 | 배열 (바이트) | 연결 (바이트) |
|---|---|---|---|---|---|---|
| 일반 이진트리 | `A(B(D,E),C(,F))` | 6 | 7 | 1 | 7 | 144 |
| 완전 이진트리 | `A(B(D,E),C(F,))` | 6 | 6 | 0 | 6 | 144 |
| 왼쪽 편향 | `A(B(C(D(E(F)))))` | 6 | 32 | 26 | 32 | 144 |
| 오른쪽 편향 | `A(,B(,C(,D(,E(,F)))))` | 6 | 63 | 57 | 63 | 144 |
| 왼쪽 편향 | `A(B(C(D(E(F(G(H(I(J)))))))))` | 10 | 512 | 502 | 512 | 240 |
| 오른쪽 편향 | `A(,B(,C(,D(,E(,F(,G(,H(,I(,J)))))))))` | 10 | 1023 | 1013 | 1023 | 240 |

### 5.3 분석

**연결 자료구조는 트리 모양과 관계없이 노드 수에만 비례한다.** 노드 6개인 트리는 일반, 완전, 편향 모두 144바이트이고, 노드 10개인 트리는 240바이트이다. 필요한 노드만 만들기 때문에 빈 자리에 대한 낭비가 없다.

**배열은 트리 모양에 따라 사용량이 크게 달라진다.** 완전 이진트리는 인덱스 1부터 n까지를 빈틈없이 채우므로 노드 6개에 6칸만 사용하고 낭비가 없다. 일반 이진트리는 중간에 빈 자리가 생긴 만큼 낭비가 생긴다(7칸 중 1칸). 편향 이진트리는 노드 하나가 내려갈 때마다 인덱스가 2배씩 커지므로 사용량이 지수적으로 늘어난다. 노드가 n개일 때 왼쪽 편향은 2^(n-1)칸, 오른쪽 편향은 2^n - 1칸이 필요하다. 측정 결과에서도 노드 6개인 오른쪽 편향 트리가 63칸 중 57칸(90%)을 비워 두고 있다.

**노드 수가 적을 때는 편향 트리라도 배열이 더 작다.** 노드 6개에서 편향 트리의 배열 사용량(32, 63바이트)은 연결 구조(144바이트)보다 작다. 배열의 한 칸은 1바이트인데 연결 구조의 노드 하나는 포인터 2개 때문에 24바이트로 24배 크기 때문이다. 그러나 노드가 10개가 되면 배열은 512, 1023바이트로 연결 구조의 240바이트를 크게 넘어선다. 위 식으로 계산하면 오른쪽 편향은 노드 8개(255 > 192), 왼쪽 편향은 노드 9개(256 > 216)부터 배열이 연결 구조보다 커진다. 이후로는 배열은 2배씩, 연결 구조는 24바이트씩 늘어나므로 차이가 빠르게 벌어진다.

**배열은 크기를 미리 정해야 한다.** 실제 프로그램에서는 트리의 높이를 미리 알 수 없으므로 배열을 65536칸(64KB)으로 고정해 두었다. 따라서 입력 트리가 작아도 항상 64KB를 차지한다. 편향 트리가 들어올 가능성을 고려하면 높이 h에 대해 2^h - 1칸을 준비해야 하므로, 배열 방식은 최악의 경우를 대비해 크게 잡아야 한다는 단점이 있다.

**결론:** 완전 이진트리이거나 노드 수가 적은 경우에는 포인터 공간이 없는 배열이 메모리를 적게 사용한다. 반대로 트리가 한쪽으로 치우치고 노드 수가 늘어날수록 배열의 사용량은 지수적으로 증가하므로 연결 자료구조가 훨씬 유리하다. 또한 이번 과제는 노드 데이터가 1바이트라 배열의 빈 칸 비용이 작았지만, 저장하는 데이터가 클수록 빈 칸 하나의 비용도 커지므로 배열 방식이 더 불리해진다.

## 6. 부모, 자식, 형제 노드 조회 비교

### 6.1 구현 방법

두 프로그램 모두 트리 출력 후 노드 이름을 하나 입력받아 부모, 왼쪽 자식, 오른쪽 자식, 형제를 출력한다.

**배열 구현**

- 파싱할 때 노드 이름과 배열 인덱스를 `charIndex[26]` 표에 기록해 두고, 입력된 노드의 인덱스 i를 바로 찾는다.
- 부모: i / 2
- 왼쪽 자식: 2i, 오른쪽 자식: 2i + 1
- 형제: i가 짝수이면 i + 1, 홀수이면 i - 1

**연결 구현**

- 파싱할 때 노드 이름과 노드 포인터를 `charNode[26]` 표에 기록해 두고, 입력된 노드를 바로 찾는다.
- 자식: `node->left`, `node->right`
- 부모: 노드에 parent 필드가 없으므로 노드만으로는 부모를 알 수 없다. 파싱할 때는 재귀 호출 인자로 부모 포인터를 알고 있으므로, 이를 `charParent[26]` 표에 따로 기록해 두고 조회한다.
- 형제: 부모를 찾은 뒤, 부모의 자식 중 자기 자신이 아닌 쪽이다.

### 6.2 분석

| 조회 | 배열 | 연결 자료구조 |
|---|---|---|
| 자식 | O(1), 곱셈 한 번 | O(1), 포인터 한 번 |
| 부모 | O(1), 나눗셈 한 번 | 노드만으로는 불가능. 루트부터 탐색하면 O(n), 이 구현처럼 별도 표를 만들어야 O(1) |
| 형제 | O(1), 덧셈 또는 뺄셈 한 번 | 부모를 먼저 알아야 계산 가능 |

배열 구현은 인덱스 하나에 부모, 자식, 형제의 위치가 모두 수식으로 정해져 있다. 그래서 추가 공간 없이 산술 연산만으로 모든 관계를 O(1)에 구할 수 있다.

연결 구현은 자식 방향으로는 포인터를 따라가면 되므로 빠르지만, 부모 방향의 정보가 노드에 없다. 부모를 O(1)에 찾으려면 이 구현처럼 별도의 표를 유지하거나 노드에 parent 포인터를 추가해야 하며, 둘 다 추가 메모리가 필요하다. 이런 준비가 없다면 부모와 형제를 찾기 위해 루트부터 트리 전체를 탐색해야 하므로 O(n)이 걸린다.

참고로 노드 이름으로 위치를 찾는 단계는 두 구현 모두 26칸짜리 표로 O(1)에 처리했다. 이는 노드 이름이 영문 대문자 26개로 제한된다는 조건 덕분에 가능한 방법이며, 이 조건이 없다면 두 구현 모두 트리를 탐색해야 한다.

### 6.3 결론

부모, 자식, 형제를 조회하는 효율은 배열 구현이 더 좋다. 배열은 트리 구조 자체가 인덱스 수식으로 표현되어 있어 추가 공간 없이 모든 관계를 O(1)에 구할 수 있지만, 연결 구현은 부모와 형제를 구하려면 추가 공간을 쓰거나 O(n) 탐색을 해야 한다. 다만 5장에서 본 것처럼 배열은 트리가 편향될수록 메모리 낭비가 커지므로, 조회 효율과 메모리 효율을 함께 고려해 트리의 모양에 맞는 구현을 선택해야 한다.